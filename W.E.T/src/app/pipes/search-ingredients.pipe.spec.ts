import { SearchIngredientsPipe } from './search-ingredients.pipe';

describe('SearchIngredientsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIngredientsPipe();
    expect(pipe).toBeTruthy();
  });
});
